export const EXPORT_SIZES = Object.freeze({
  small: Object.freeze({ width: 800, height: 600 }),
  medium: Object.freeze({ width: 1000, height: 700 }),
  large: Object.freeze({ width: 1200, height: 800 })
});

export const exportSizes = EXPORT_SIZES;
export default EXPORT_SIZES;
