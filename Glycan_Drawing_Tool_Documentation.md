# 🧩 Glycan Drawing Tool 说明文档

## 📘 简介

**Glycan Drawing Tool** 是一个基于网页的糖链结构绘制与编辑工具。  
用户可以通过可视化界面轻松绘制单糖节点、连接关系以及文字注释，并根据 SNFG（Symbol Nomenclature for Glycans）标准自动生成标准符号表示。交互逻辑与UI界面更加直观，优于现有的其他工具。

---

## 🧱 功能概述

界面由三个主要部分组成：

- **左侧面板**：工具栏（选择、添加糖、添加文字、添加预设糖质、橡皮擦）
- **中间画布**：主绘图区域（支持缩放、拖动）
- **右侧面板**：糖形状、颜色、连接样式、文字样式等详细自定义选项
- **顶部面板**：画布操作（撤回/重做、清空画布、导出、画布缩放）

---

## 🧭 工具栏说明

| 工具 | 图标 | 功能说明 |
|------|------|-----------|
| 选择 (`select`) | ↖️ | 选择已有元素进行移动、复制或样式修改 |
| 添加糖 (`add`) | 🍬 | 在画布中点击放置新的单糖符号 |
| 添加文字 (`text`) | 📝 | 在画布中添加文字注释或标签 |
| 预设糖质 (`preset`) | 🧩 | 从内置模板中插入完整糖链结构 |
| 橡皮擦 (`delete`) | 🧽 | 删除选中的元素 |

---
## 🎨 添加糖模式

可进行的操作：
- **添加单糖** 在添加糖模式下，通过点击在画布中添加糖。  
- **定向添加** 可以通过长按已添加的糖并向空白位置拖动，在固定角度（30°/45°）添加新糖。  
- **添加键连** 长按已添加的糖并向另一糖拖动，可以建立两个糖的连接。  
- **预设样式** 选择模式下可以调节的糖和连接样式（下述）均可在此处预先调整并直接添加。  

## 🍬 糖结构样式

### 1. SNFG 糖预设
右侧面板提供常见单糖预设（Glc、Gal、Man、GlcNAc、GalNAc、Fuc、GlcA、Sia、Xyl），  
符合SNFG 2.0标准，仅配置颜色与形状，不影响自定义大小。

### 2. 自定义糖型
可自定义：
- **形状**（圆、方、菱形、三角形、星形、括号、自由端等）
- **填充颜色与透明度**（已预设好符合SNFG 2.0标准的颜色，也可以使用十六进制RGB自定义）
- **尺寸大小**  
- **边缘**（宽度、样式、颜色、透明度）
---

## 🔗 连接与Linkage文字

可自定义：
- **Linkage信息**（输入完整的连接如α2-6，或简化的连接如b14）
- **Linkage显示**（勾选即可将linkage信息添加至键右侧，方向符合SNFG 2.0标准，数字靠近自由端。通过互换按钮可以反转键连）
- **连接线条样式**（宽度、样式、颜色、透明度）
- **Linkage文字样式**（字号、字体、样式、颜色、透明度）

---

## ✏️ 文本添加与样式

通过“添加文字”工具可在任意位置插入文本标签。  
支持调整：
- 字号与字体
- 粗体 / 斜体 / 下划线
- 颜色与透明度

---

## 🧩 预设寡糖模板

支持插入完整的糖链 SVG 模板文件（`presetGlycanToolbar`）。
用户点击预设缩略图后，点击画布即可放置模板。

---

## 🧮 撤销与重做

内置完整的撤销/重做系统：

- **撤销 (Undo)**：回退上一步操作  
- **重做 (Redo)**：恢复被撤销的步骤  
- 最多保存 50 步历史记录  

包括对元素样式变化、位置移动、添加与删除等所有修改。

---

## 🖼️ 导出与画布控制

### 导出选项：
| 格式 | 描述 |
|------|------|
| **SVG** | 可编辑矢量格式，保留全部图形信息 |
| **PNG** | 带透明背景的高分辨率位图 |
| **JPG** | 适合展示的白底位图 |

### 导出尺寸：
- 小（800×600）
- 中（1000×700）
- 大（1200×800）

### 画布操作：
- 拖动：使用鼠标拖动画布
- 缩放：滑块控制 50%–300%
- 重置：恢复至 100%

---

## 🧰 其他功能

- **批量选择**：框选多个元素进行统一调整、Shift+点击进行精准多选  
- **连续擦除**：长按橡皮擦可快速删除多个元素  
- **快捷键支持**：
  - `Ctrl/Cmd + Z/Y`：撤销/重做  
  - `Ctrl/Cmd + B/I/U`：文字的加粗/斜体/下划线  
  - `Ctrl/Cmd + C/X/V/A`: 复制/剪切/粘贴/全选
  - `Delete`：删除选中元素  
  - `Esc`：退出当前模式（如预设放置）

---

## 🧑‍💻 技术实现

- **前端框架**：纯原生 JavaScript + SVG 操作  
- **主类结构**：`GlycanDrawer`  
  - 管理画布、事件绑定、元素绘制、撤销重做系统等  
- **核心逻辑模块**：  
  - `setupToolbar()` 工具栏交互  
  - `setupStyleControls()` 样式控制与绑定  
  - `initializeWorkspace()` 初始化画布  
  - `exportCanvas()` 导出逻辑  
  - `handleMouseDown/Move/Up()` 用户交互响应  

---

## 🚀 未来计划

- **图文互转**：将标准化的IUPAC糖质结构命名与标准化的SNFG糖质图形表示互相转化  
- **结构整理**：一键整洁绘制的糖质结构  

---

## 📄 联系与帮助

- **开发者**：x-yguo@pku.edu.cn  
- **参考标准**：[NCBI SNFG 2.0](https://www.ncbi.nlm.nih.gov/glycans/snfg2_0.html)  
